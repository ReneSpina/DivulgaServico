@model IEnumerable<DIVULGA_SERVICOS.Models.CAD_CLIENTE>

@{
    ViewBag.Title = "Gerenciamento de Clientes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Gerenciamento de Clientes</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CAD_PES_JURIDICA.CAD_PESSOA.NM_NOME_PESSOA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NM_NOME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CAD_PES_JURIDICA.CAD_PESSOA.NM_NOME_PESSOA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NM_NOME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>